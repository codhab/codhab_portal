.row.mini-margin
  .col-md-9.box
    .box_internal
      .post_meta
        = render_shareable :buttons=> ['twitter', 'facebook','google_plus'], :facebook=> {:send=> 'true', :url=> "#{request.original_url}"}
        .post_date
          -# = @page.date.strftime('%d/%m/%Y')
      .post{:style => "color:#333333;"}
        .imagem_post
          - if @page.active_photo? && @page.photo_author?
            = image_tag @page.thumbnail , :alt => "#{(@page.photo_description.present?) ? @page.photo_description : 'Foto sem descrição'}"
        .author_custom
          - if @page.photo_description? && @page.photo_author?
            = @page.photo_description
            %span
              Autor:
              = @page.photo_author.html_safe
        - if (@page.id == 173)
          %p.bg-danger{:style => "color: #fff;width: 95%;margin: 0 auto;padding: 25px;font-size: 20px;border-radius: 3px;font-weight: bold;text-align: center;"}
            A CODHAB informa que os candidatos do Programa Habita Brasília, eixo Morar Bem, que foram indicados a partir do dia 3 de setembro de 2016, não devem pagar qualquer valor adicional às unidades do Riacho Fundo II, 4ª etapa. Os demais candidatos, que fecharam contratos diretamente com a AMMVS ou fizeram parte da migração entre maio e setembro do mesmo ano, devem seguir a as cláusulas contratuais.
        .content_post
          %h1.post_title
            = @page.title
          %p.post-post
            = @page.content.html_safe
          - if @page.report?
            %p.post-post
              = @page.report.html_safe
          %a.button_back{"arial-label" => "back to top", :href => "#"} VOLTAR AO TOPO
  = render('layouts/portal/sidebar')
