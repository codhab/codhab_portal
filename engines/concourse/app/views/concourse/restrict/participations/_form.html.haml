= simple_form_for @participation, url: project_restrict_participations_path(project_id: @project) do |f|
  .row
    .col-md-12
      %h4 Adicione os dados da sua equipe abaixo:
  .row
    .col-md-12
      = f.simple_fields_for :team_participations do |builder|
        = render 'team_participation_fields', f: builder
  .row
    .col-md-12
      = link_to_add_fields "+ Adicionar membro", f, :team_participations
  %hr/
  .row
    .col-md-12
      %h4 Projeto
  .row
    .col-md-10
      = f.input :archive_one, label: "06 pranchas A2, em formato PDF, dispostas em arquivo único, com tamanho máximo de 30MB", as: :file
  .row
    .col-md-10
      = f.input :archive_two, label: "01 prancha A1, em formato PDF, com tamanho máximo de 5MB", as: :file
  .row
    .col-md-10
      = f.input :archive_three, label: "01 imagem ilustrativa, em formato PNG ou JPG, com tamanho máximo de 5MB", as: :file
  .row
    .col-md-12
      = @project.text_participation.html_safe
  %hr/
  .row
    .col-md-12
      = f.submit "Enviar meu projeto", class: 'btn btn-success', data: { disable_with: t(:disable_with)}
