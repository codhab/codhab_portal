.row
  .col-md-6
    %h3
      %b Endereço de Reserva
    %p= @enterprise_unit.complete_address
    .row
      .col-md-6
        = simple_form_for @booking, url: bookings_path do |f|
          = f.input :unit_id, as: :hidden,  input_html:{ :value =>  @enterprise_unit.id }
          .row
            .col-md-12
              = f.input :cpf, input_html: { class: 'cpf'}
          .row
            .col-md-12
              = f.label "Observação"
              = f.input :observation, as: :text, label: false
          .row
            .col-md-12
              = f.label "Arquivo"
              = f.input :file_path, as: :file, label: false
          %hr
          .row
            .col-md-12
              = f.button :submit, "Reservar", class: 'btn btn-success', data: { disable_with: t(:disable_with)}
  .col-md-6
    %h2
      %b Atenção
    %p
      Atente-se ao CPF informado.
    %p
      Atente-se ao Endereço selecionado.
    %p
      Verifique se o CPF informado está correto e esta vínculado ao candidato destino desta reserva.

    %p Esta operação é irreversível.
