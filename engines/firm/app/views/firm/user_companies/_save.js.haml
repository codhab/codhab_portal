- if @user_company.errors.any?
  $('#modal .modal-content').html("#{escape_javascript(render 'new')}")
- else
  $('#modal').modal('hide')
  $('.index-company').html("#{escape_javascript(render 'index')}")