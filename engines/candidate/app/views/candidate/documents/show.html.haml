.row.mini-margin
  .col-md-12
    .panel.panel-default
      .panel-body
        .row
          .col-md-12
            %h2{style: 'text-align: center;'}
              %b Validador de documentos
        .row
          .col-md-12
            %table.table
              %thead
                %tr
                  %th Edital
                  %th Enidade
                  %th CNPJ
              %tbody
                - if @excel.present?
                  - row = @excel.checker.gsub('"', "").gsub("[","").gsub("]","").split(",")
                  %tr
                    %td= row[0]
                    %td= row[1]
                    %td.collapsing= row[2]
              %thead
                %tr
                  %th{colspan: 2} Pesidente
                  %th CPF
              %tbody
                - if @excel.present?
                  - row = @excel.checker.gsub('"', "").gsub("[","").gsub("]","").split(",")
                  %tr
                    %td{colspan: 2}= row[3]
                    %td.collapsing= row[4]
                    
              %thead
                %tr
                  %th{colspan: 3} Endereços
              %tbody
                - if @excel.present?
                  - row = @excel.checker.gsub('"', "").gsub("[","").gsub("]","").split(",")
                  %tr
                    %td{colspan: 3}= row[6..row.length].split(',').join(',')
                      