.row
  .col-md-6.col-md-offset-4.form
    = simple_form_for @password, url: update_question_three_remember_passwords_path, remote: true do |f|
      = f.input :response_three, label: "Selecione o último telefone informado", as: :radio_buttons,
        collection: f.object.questions_three_formated, wrapper: :vertical_radio_and_checkboxes

      = f.submit "Prosseguir", class: 'btn btn-success', data: { disable_with: t(:disable_with)}