.col-md-8.col-md-offset-2
  = simple_form_for [@cadastre, @responsible], url: cadastre_responsibles_path(@cadastre) do |f|
    .row
      .col-md-6
        = f.input :members_id, label: 'Responsáveis', as: :select, collection:  Core::SocialWorkCadastre::CadastreMember.where(cadastre_id: @cadastre.id), include_blank: false
    .row
      .col-md-6
        = f.button :submit, "Realizar cadastro"
