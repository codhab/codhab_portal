
%div{class: (controller_name != 'home') ? 'container main' : '' }
  .container
    .row
      .col-md-12.no_padding
        .gallerys_title
          %span
            QUEM FAZ A DIFERENÇA

    .row
      .col-md-12.no_padding.box_internal
        - @galerias.each do |galeria|
          .col-md-1
            .date_g
              %span
                = galeria.date.strftime('%d-%m')
          .col-md-11.box_galerias{:id => "item_#{galeria.link}"}
            %h2
              = galeria.title
            %p
              = galeria.content.html_safe
            - if galeria.flickr
              = galeria.flickr.html_safe
      %hr
