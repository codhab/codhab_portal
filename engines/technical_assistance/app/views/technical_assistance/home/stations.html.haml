.container{:style => "background: #ffffff"}
  .row
    .col-md-12
      .slider_tech.technical_slider
        .bxslide_action{:style => "margin-top:30px;height:400px;"}
          = image_tag '/portal/s-20.png'
          = image_tag '/portal/s-21.png'
          = image_tag '/portal/s-22.png'
          = image_tag '/portal/s-23.png'
          = image_tag '/portal/s-24.png'
  .row
    .col-md-12
      %h1
        %b
          = image_tag "/portal/#{@station.id}-station_posto.jpg", style:'width:100%'
  .row
    .col-md-12.tech_bloco_2
      .col-md-8.center{:style => "width: 100%; margin-top: 20;"}
        %h2{:style => "color: #fff"}
          %b
            = @station.name
            %br
            = "(#{@station.city.name}/DF)"
        %p.opened2
          = @station.date_open
        %h4
          %b
            = @station.status ? "Posto Ativo" : "Posto Inativo"
            %br
          = @station.schedule
        %h4
          %b
            Endereço
            %br
          = @station.address
  .row
    .col-md-6.margint50
      .block_station
        .text_station
          %h1
            %b
              assistência técnica
          %br
          -if @member_resp_1.present?
            - @member_resp_1.each do |resp|
              %tr
                %td
                  %b{:style => "font-size: 24px;"}
                    -unless resp.previous_coordination.present?
                      - attendant = resp.staff.name.to_s
                      - attendant = attendant.split(' ')
                      = resp.formation? ? 'Eng.' : 'Arq.'
                      = attendant.first.capitalize
                      = attendant.last.capitalize
                    -else
                      = resp.formation? ? 'Eng.' : 'Arq.'
                      = resp.previous_coordination
                %br
                %td= "#{resp.formation? ? 'CREA:' : 'CAU:'} #{resp.crea}"
              %br
          -if @member_esta_1.present?
            %h3
              %b
                Estagiários
            - @member_esta_1.each do |esta|
              %tr
                %td
                  -unless esta.previous_coordination.present?
                    - attendant = esta.staff.name.to_s
                    - attendant = attendant.split(' ')
                    = attendant.first.capitalize
                    = attendant.last.capitalize
                    ="(#{esta.formation? ? 'Eng.' : 'Arq.'})"
                  -else
                    = esta.previous_coordination
                    ="(#{esta.formation? ? 'Eng.' : 'Arq.'})"
              %br
          -if @member_volu_1.present?
            %h3
              %b
                Voluntários
            - @member_volu_1.each do |volu|
              %tr
                %td
                  %b
                    -unless volu.previous_coordination.present?
                      - attendant = volu.staff.name.to_s
                      - attendant = attendant.split(' ')
                      = volu.formation? ? 'Eng.' : 'Arq.'
                      = attendant.first.capitalize
                      = attendant.last.capitalize
                    -else
                      = volu.formation? ? 'Eng.' : 'Arq.'
                      = volu.previous_coordination
                %td= "#{volu.formation? ? 'CREA:' : 'CAU:'} #{volu.crea} | #{volu.date_service}"
              %br
          -if @member_coor_1.present?
            %h3
              %b
                Coordenação Anterior
            - @member_coor_1.each do |coor|
              %tr
                %td
                  %b
                    -unless coor.previous_coordination.present?
                      - attendant = coor.staff.name.to_s
                      - attendant = attendant.split(' ')
                      = coor.formation? ? 'Eng.' : 'Arq.'
                      = attendant.first.capitalize
                      = attendant.last.capitalize
                    -else
                      = coor.formation? ? 'Eng.' : 'Arq.'
                      = coor.previous_coordination
                %br
                %td= "#{coor.formation? ? 'CREA:' : 'CAU:'} #{coor.crea} | #{coor.date_service}"
              %br
    .col-md-6.margint50
      .block_station
        .text_station
          %h1
            %b
              regularização fundiária
          %br
          -if @member_resp_2.present?
            - @member_resp_2.each do |resp|
              %tr
                %td
                  %b{:style => "font-size: 24px;"}
                    -unless resp.previous_coordination.present?
                      - attendant = resp.staff.name.to_s
                      - attendant = attendant.split(' ')
                      = resp.formation? ? 'Eng.' : 'Arq.'
                      = attendant.first.capitalize
                      = attendant.last.capitalize
                    -else
                      = resp.formation? ? 'Eng.' : 'Arq.'
                      = resp.previous_coordination
                %br
                %td= "#{resp.formation? ? 'CREA:' : 'CAU:'} #{resp.crea}"
              %br
          -if @member_esta_2.present?
            %h3
              %b
                Estagiários
            - @member_esta_2.each do |esta|
              %tr
                %td
                  -unless esta.previous_coordination.present?
                    - attendant = esta.staff.name.to_s
                    - attendant = attendant.split(' ')
                    = attendant.first.capitalize
                    = attendant.last.capitalize
                    ="(#{esta.formation? ? 'Eng.' : 'Arq.'})"
                  -else
                    = esta.formation? ? 'Eng.' : 'Arq.'
                    ="(#{esta.formation? ? 'Eng.' : 'Arq.'})"
              %br
          -if @member_volu_2.present?
            %h3
              %b
                Voluntários
            - @member_volu_2.each do |volu|
              %tr
                %td
                  %b
                    -unless volu.previous_coordination.present?
                      - attendant = volu.staff.name.to_s
                      - attendant = attendant.split(' ')
                      = volu.formation? ? 'Eng.' : 'Arq.'
                      = attendant.first.capitalize
                      = attendant.last.capitalize
                    -else
                      = volu.formation? ? 'Eng.' : 'Arq.'
                      = volu.previous_coordination
                %td= "#{volu.formation? ? 'CREA:' : 'CAU:'} #{volu.crea} | #{volu.date_service}"
              %br
          -if @member_coor_2.present?
            %h3
              %b
                Coordenação Anterior
            - @member_coor_2.each do |coor|
              %tr
                %td
                  %b
                    -unless coor.previous_coordination.present?
                      - attendant = coor.staff.name.to_s
                      - attendant = attendant.split(' ')
                      = coor.formation? ? 'Eng.' : 'Arq.'
                      = attendant.first.capitalize
                      = attendant.last.capitalize
                    -else
                      = coor.formation? ? 'Eng.' : 'Arq.'
                      = coor.previous_coordination
                %br
                %td= "#{coor.formation? ? 'CREA:' : 'CAU:'} #{coor.crea} | #{coor.date_service}"
          -if @member_3.present?
            %h1
              %b
                acompanhamento obras de infraestrutura
            - @member_3.each do |member|
              %tr
                %td
                  %b{:style => "font-size: 24px;"}
                    -unless member.previous_coordination.present?
                      - attendant = member.staff.name.to_s
                      - attendant = attendant.split(' ')
                      = member.formation? ? 'Eng.' : 'Arq.'
                      = attendant.first.capitalize
                      = attendant.last.capitalize
                    -else
                      = member.formation? ? 'Eng.' : 'Arq.'
                      = member.previous_coordination
                %br
                %td= "#{member.formation? ? 'CREA:' : 'CAU:'} #{member.crea}"
              %br
  .row.margint50
    .col-md-12
      #map
        %iframe{:src => "https://www.google.com/maps/d/embed?mid=1yFxEMyRBUNR496HgVBXVV7hiN2tUnibf&ll=#{@station.latitude}%2C#{@station.longitude}&z=14", :width => "100%",:height => "480", }
