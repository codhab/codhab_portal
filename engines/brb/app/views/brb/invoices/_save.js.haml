- if @invoice.errors.any?
  $('.ui.modal').html("#{escape_javascript(render 'new')}")
- else
  $('.index-invoice').html("#{escape_javascript(render 'index')}")

  js_update();

  $('.ui.modal').modal('hide')
